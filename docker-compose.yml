version: '3.9'

services:
  backend:
    build:
      context: ./backend
    ports:
      - '8000:8000'
    volumes:
      - ./backend:/app

  renderer:
    build:
      context: ./renderer
    volumes:
      - ./renderer:/app